using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BurnedEgg : Resource
{
    // Start is called before the first frame update
    void Start()
    {
        name = "burned egg";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
