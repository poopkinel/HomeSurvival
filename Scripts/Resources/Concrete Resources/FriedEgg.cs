using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FriedEgg : Resource
{
    // Start is called before the first frame update
    void Start()
    {
        name = "fried egg";

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
