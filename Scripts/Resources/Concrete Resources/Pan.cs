using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pan : Resource
{
    // Start is called before the first frame update
    void Start()
    {
        name = "pan";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
